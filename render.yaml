services:
  - type: web
    name: devops-235-svirgunov
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: DEBUG
        value: "false"
      - key: PORT
        value: "5000"
      - key: SECRET_KEY
        generateValue: true
    healthCheckPath: /health
